// ?使用mock模拟数据
// * mockjs的特点:1.可以拦截本地的ajax请求 2.全自动Random特性(随机生成)
// mockjs支持es6/cjs/amd/cmd

// *mockjs的语法规范包括两部分：1.数据模板定义规范(DTD) 2.数据占位符定义规范(DPD)

// ?注意事项
// !mockjs只能拦截ajax请求，不能拦截fetchAPI的请求
// !项目上线之前一定要把mock干掉

// todo mock的包的体积是很大的，不建议在前端中直接使用,推荐结合node.js+mockjs使用
